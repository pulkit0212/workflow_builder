# Docker

Add multi-stage Dockerfiles per `apps/*` and `python-services/*` (see RFC §25).

Suggested compose overlay: Kafka, Redis, MongoDB alongside existing project `docker-compose.yml` at repo root.
